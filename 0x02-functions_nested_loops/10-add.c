#include "main.h"

/**
 * add - a function that adds two integers and
 * returns the result
 *
 * @a: the first argument
 * @b: the second argument
 *
 * Return: the addition
 */
int add(int a, int b)
{
	return (a + b);
}
