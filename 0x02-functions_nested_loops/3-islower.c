#include "main.h"

/**
 * _islower: checks for lowercase charachter
 *
 * @c: int that used for chacking the case
 *
 * return: always 0
 */
int _islower(int c)
{
	if (c >= 97)
		return (1);

	return (0);
}
