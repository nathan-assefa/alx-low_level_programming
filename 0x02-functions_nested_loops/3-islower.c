#include "main.h"

/**
 * main - Entry point
 *
 * int _islower: only runs when the 
 * charchter is lowercase
 * return: always 0
 */
int _islower(int c)
{
	if (c >= 97)
		return (1);

	return (0);
}
