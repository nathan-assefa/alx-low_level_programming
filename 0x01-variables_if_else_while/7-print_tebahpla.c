#include <stdio.h>
#include <string.h>

/**
 * mani - Entery point
 *
 * Return: 0 when the progam is successful
 */
int main(void)
{
char lower = a;

while (lower <= 'z')
{
putchar(strrev(lower));
lower++
}

putchar('\n');
return (0);
}
