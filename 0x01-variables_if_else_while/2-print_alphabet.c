#include <stdio.h>

/**
 * main - printing
 *
 * Return: 0 when the program succeed
 */
int main(void)
{
char alphabet = 'a';
while(alphabet <= 'z')
{
putchar(alphabet);
}

putchar('\n');

return (0);
}
