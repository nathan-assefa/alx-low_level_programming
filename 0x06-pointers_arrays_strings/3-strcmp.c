#include "main.h"
#include <string.h>

/**
 * _strcmp- comparing strings
 *
 * @s1: Pointer
 * @s2: Pointer to a string s2
 * Return: s1
 *
 */

int _strcmp(char *s1, char *s2)
{
	int n;
	strcmp(s1, s2, n);
	return (s1);
}
